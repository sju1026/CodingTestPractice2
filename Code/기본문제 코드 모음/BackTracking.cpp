#include <iostream>
#define MAX 9

using namespace std;

/*int n, m;
int rs[MAX] = { 0, };
bool chk[MAX] = { false, };

void Recur(int num) {
	if (num == m)
	{
		for (int i = 0; i < m; i++)
		{
			cout << rs[i] << ' ';
		}
		cout << '\n';
		return;
	}
	for (int i = 1; i <= n; i++)
	{
		if (chk[i] == false)
		{
			chk[i] = true;
			rs[num] = i;
			Recur(num + 1);
			chk[i] = false;
		}
	}
}

void main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> n >> m;

	Recur(0);
}*/