#include <iostream>
#include <vector>

using namespace std;

/*void main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n;
	vector<int> rs = { 0, 1, 2 };

	cin >> n;

	for (int i = 3; i < n + 1; i++)
		rs.push_back((rs[i - 1] + rs[i - 2] % 10007));

	cout << rs[n];
}*/